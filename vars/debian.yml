---
docker_apt_repo:
  key_uri: "https://download.docker.com/linux/{{ ansible_distribution | lower }}/gpg"
  repo_uri: "https://download.docker.com/linux/{{ ansible_distribution | lower }}"
  repo_filename: docker
  suite: "{{ ansible_distribution_release }}"
  comp: stable
